import { motion } from "framer-motion";

export default function Box() {
     return (
          <motion.div style={{ background: 'blue', width: '200', height: '200'}}>

          </motion.div>
     )
}